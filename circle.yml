machine:
  environment:
    PYTHONPATH: projects
  python:
    version: 3.5.0
  node:
    version: 8.3.0

dependencies:
  pre:
    - pip3 install -r projects/requirements.txt

test:
  override:
    - pep8 projects/semantic_selector/*.py
    - python ./projects/tests/test_*.py
    - npm run danger
